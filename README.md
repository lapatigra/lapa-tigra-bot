# 🎸 LAPA TIGRA — Бот заказа песен

## Как задеплоить на Railway

### Шаг 1 — Загрузи на GitHub

```bash
cd lapa-tigra-bot
git init
git add .
git commit -m "init"
git branch -M main
git remote add origin https://github.com/ТВО_ИМЯ/lapa-tigra-bot.git
git push -u origin main
```

### Шаг 2 — Railway

1. Зайди на [railway.app](https://railway.app)
2. **New Project → Deploy from GitHub repo**
3. Выбери репозиторий `lapa-tigra-bot`
4. Railway сам задеплоит!

### Шаг 3 — Переменные окружения

В Railway → твой проект → **Variables** добавь:

| Переменная | Значение |
|---|---|
| `BOT_TOKEN` | токен твоего бота от @BotFather |
| `OWNER_CHAT_ID` | `954676667` (твой Telegram ID) |

### Шаг 4 — Получи URL сайта

В Railway → **Settings → Networking → Generate Domain**
Получишь что-то вроде: `https://lapa-tigra-bot.up.railway.app`

### Шаг 5 — Настрой Mini App в @BotFather

1. Напиши @BotFather
2. `/mybots` → выбери `@order_songs_bot`
3. **Bot Settings → Menu Button → Configure menu button**
4. Вставь URL твоего Railway сайта
5. Название кнопки: `🎵 Заказать песню`

### Шаг 6 — Добавь фото

Загрузи файл `IMG_4643.jpeg` в папку `public/` рядом с `index.html`

---

## Как это работает

1. Человек открывает бота `@order_songs_bot`
2. Нажимает кнопку меню → открывается Mini App
3. Выбирает песню → пишет поздравление → нажимает "Оплатить"
4. **Тебе приходит сообщение** с именем, username и заказом
5. Человек переходит на Яндекс Типс и платит 1000₽
6. Ты можешь написать ему в Telegram через @username
